<h1>Current Client Information</h1>

<p>Name: <%= @client.full_name %></p>
<p>Age: <%= @client.age %></p>
<p>Notes: <%= @client.notes %></p>

<form action="/clients/<%= @client.id %>" method="post">
  <input type="hidden" name="_method" id="hidden" value="patch">

<h2>Update client information</h2>

  <label for="client[full_name]">Client Name: </label>
  <input type="text" name="client[full_name]" id="client_full_name">
  <br><br>
  <label for="client[age]">New Age: </label>
  <input type="text" name="client[age]" id="client_age">
  <br><br>
  <label for="client[notes]">Add Notes: </label>
  <textarea rows="4" cols="23"></textarea>
  <br><br>

  <h3>Select course(s):</h3>
  <% @courses.each do |course| %>
  <input type="checkbox" name="client[course_ids][]" id="course_<%= course.id %>" value="<%= course.id %>"<%= 'checked' if @client.courses.include?(course) %>><%= course.name %></input>
  <% end %>

  <h3>and/or, create a new course:</h3>
  <label for="course[name]">Course Name:</label>
  <input type="text" name="course[name]" id="new_course">
  <br><br>

  <input type="submit" value="Edit Client">
</form>
